<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h3>Registered Dogs:</h3>
    <ul>
    <% for(var i = 0; i < name.length; i++) { %>
        <li>Registered Name: <%= name[i].registeredName %>, Call Name: <%= name[i].callName %>, Sanction ID: <%= name[i].sanctionId %></li>
      <% } %>
    </ul>
</body>
</html> 